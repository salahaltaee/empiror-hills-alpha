{
  "name": "emperor-hills-alpha-functions",
  "engines": { "node": "18" },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,hosting,storage",
    "deploy": "firebase deploy --only functions",
    "lint": "echo no-lint"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1"
  }
}
